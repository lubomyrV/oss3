/**
 * Service Layer 
 * The service layer is home to the business logic within the application. 
 * Majority of code will live here. 
 * The service layer will interact with Domain and Selector classes, as well as other Service classes as needed.
 * Most of DML operations runs from the service layer.
 * This layer must run with sharing
 */
public interface IService {
    
}